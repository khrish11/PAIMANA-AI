# Backend

FastAPI application for the API layer plus online Processing and ML/AI services. Business logic lives in `backend/app/services/` as plain Python modules so it can be unit-tested and reused without FastAPI route decorators.
